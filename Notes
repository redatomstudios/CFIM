1. Status - On-Going??
2. Separate Table for sectors(Since, it has sub-sectors)
3. Status of member - Can admin edit this? Or is it fixed(like in status of Projects)? Just need to know if we need to add an extra table for that?